# SEO

Phase 5 adds the SEO foundation:

- Root metadata.
- Open Graph metadata.
- Twitter card metadata.
- Schema.org website JSON-LD.
- `robots.ts`.
- `sitemap.ts`.
- Accessible image and map fallback labels.

Page-specific metadata can be expanded as real content and backend data become available.
